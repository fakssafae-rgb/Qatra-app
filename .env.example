# Copy to .env and fill in values before running locally
FLASK_ENV=development
SECRET_KEY=change-me-to-a-long-random-string
DATABASE_URL=sqlite:///instance/qatra.db
# For production on Render, DATABASE_URL is set automatically if you add a PostgreSQL addon
